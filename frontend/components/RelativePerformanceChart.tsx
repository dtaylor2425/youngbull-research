"use client";
import {useMemo,useState} from "react";
import {CartesianGrid,Legend,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from "recharts";
import type {ComparisonResponse} from "@/lib/types";
export function RelativePerformanceChart({data,ticker}:{data:ComparisonResponse;ticker:string}){const [range,setRange]=useState<"3M"|"6M"|"1Y"|"2Y">("1Y");const days={"3M":66,"6M":132,"1Y":260,"2Y":520};const rows=useMemo(()=>{const dates=new Set<string>();Object.values(data.series).forEach(s=>s.slice(-days[range]).forEach(r=>dates.add(r.date)));const maps=Object.fromEntries(Object.entries(data.series).map(([k,s])=>[k,new Map(s.map(r=>[r.date,r.value]))]));return Array.from(dates).sort().map(date=>({date,[ticker]:maps[ticker]?.get(date),SPY:maps.SPY?.get(date),SMH:maps.SMH?.get(date)}))},[data,range,ticker]);return <div className="relative-chart-shell"><div className="chart-controls"><div>{(["3M","6M","1Y","2Y"] as const).map(x=><button key={x} className={range===x?"active":""} onClick={()=>setRange(x)}>{x}</button>)}</div><small>Indexed to 100</small></div><div className="relative-chart"><ResponsiveContainer width="100%" height="100%"><LineChart data={rows}><CartesianGrid stroke="#292824" vertical={false}/><XAxis dataKey="date" minTickGap={45} tickLine={false} axisLine={false}/><YAxis tickLine={false} axisLine={false}/><Tooltip/><Legend/><Line type="monotone" dataKey={ticker} stroke="#d8a63f" strokeWidth={2.3} dot={false}/><Line type="monotone" dataKey="SPY" stroke="#bdb8aa" dot={false}/><Line type="monotone" dataKey="SMH" stroke="#77736b" dot={false}/></LineChart></ResponsiveContainer></div></div>}
